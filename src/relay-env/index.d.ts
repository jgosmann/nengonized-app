import { Environment } from 'relay-runtime'

export function init(): void
export let query_ws: WebSocket
export let subscription_ws: WebSocket
export let environment: Environment
export default environment
